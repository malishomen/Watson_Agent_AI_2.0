Окей, Ватсон-режим «Cursor, делай красиво, пока не заработает» включён 😄

Ниже — **готовый файл для Cursor**. Просто сохрани как
`D:\projects\Ai-Agent_Watson\Watson_Agent_2.0\cursor_tasks\FIX_PIPELINE_INSTRUCTIONS.md`
и открой его в Cursor, дальше уже он пусть пашет.

---

````markdown
# 🧠 GLOBAL TASK: починить LLM-конвейер + Cursor и оптимизировать workspace

Ты работаешь в проекте **Watson_Agent_2.0** на Windows.  
Цель: привести в полностью рабочее состояние конвейер

**Telegram → /relay/submit → Qwen (LM Studio) → inbox/*.task.json → cursor_task_sender.py → cursor_tasks/*.md → Cursor AI**,  

и при этом **уменьшить нагрузку на аналитку кода (Pyright / basedpyright)**, чтобы не было долгого «Enumeration of workspace source files…».  
Документация по конвейеру уже есть — используй её, а не выдумывай архитектуру заново.

---

## 0. Сначала — оптимизировать workspace (исправить “Enumeration of workspace source files…”)

1. **Определи корень проекта**:  
   Используй в качестве корня путь вида:  
   `D:\projects\Ai-Agent_Watson\Watson_Agent_2.0`  
   и НЕ считай весь диск/домашнюю директорию частью workspace.

2. Если в корне **нет** `pyrightconfig.json` — создай.  
   Если есть — аккуратно дополни.

3. Добавь в `pyrightconfig.json` секцию `exclude`, чтобы исключить тяжёлые папки, не относящиеся к коду агента, например:

   ```json
   {
     "exclude": [
       "**/node_modules",
       "**/.git",
       "**/.github",
       "**/.venv",
       "**/venv",
       "**/__pycache__",
       "logs",
       "dist",
       "build",
       "tmp",
       "temp"
     ]
   }
````

4. Убедись, что basedpyright/pyright теперь индексирует только рабочие каталоги проекта, а не весь диск.

---

## 1. Прочитать и усвоить встроенную документацию

Найди и внимательно прочитай (без переписывания логики с нуля):

* `QUICK_START_LLM_CURSOR.md` — быстрый старт Qwen 2.5 → Cursor pipeline. 
* `LLM_PIPELINE_WORKING.md` — описание рабочей архитектуры: Telegram → Qwen → inbox → Cursor. 
* `CONVEYOR_FIXED_SUMMARY.md` — что было сломано и как уже чинили конвейер. 
* `CONVEYOR_TO_CURSOR_GUIDE.md` — подробное руководство по file-based интеграции с Cursor. 

Сделай для себя **внутренний конспект**:

* какие скрипты и файлы являются основными:

  * `scripts\Start-WatsonApi.ps1` (запуск API),
  * `scripts\telegram_bridge.py`,
  * `scripts\cursor_task_sender.py`,
  * `scripts\make_task.ps1`,
  * `START_FULL_SYSTEM.ps1`,
  * `api\fastapi_agent.py` (или аналогичный main/agent-файл);
* какая модель используется (Qwen 2.5 Coder, LM Studio, base URL `http://127.0.0.1:1234/v1` и т.п.); 
* как устроены папки `inbox\` и `cursor_tasks\`. 

**Важно:** опирайся на текущий дизайн — он уже описан как «WORKING/PRODUCTION READY». Задача — не перепридумать, а довести до реально стабильного состояния.

---

## 2. Проверить запуск API и базовое здоровье

Открой и проанализируй файлы логов:

* `uvicorn_8090.err.log`
* `uvicorn_8090.out.log`

Убедись, что:

1. Сервер Uvicorn стартует без ошибок:

   * есть строки вида:
     `Application startup complete.`
     `Uvicorn running on http://127.0.0.1:8090` 

2. `/health` отдаёт 200 OK (это уже видно в логах):
   `GET /health HTTP/1.1" 200 OK` 

3. Если есть трассировки ошибок — проанализируй и **исправь их в коде**, пока логи не станут чистыми при обычном запуске.

Если обнаружишь расхождение между документацией и фактическим кодом (эндпоинты, пути, переменные окружения) — **приводи код в соответствие с документацией**, если нет явной причины делать иначе.

---

## 3. Привести конвейер LLM в стабильное рабочее состояние

Твоя цель — чтобы полностью работал полный путь:

**Telegram / make_task.ps1 → /relay/submit → Qwen → inbox/*.task.json → cursor_task_sender.py → cursor_tasks/*.md**

### 3.1. Проверить /relay/submit

1. Найди реализацию эндпоинта `/relay/submit` (обычно в `api\fastapi_agent.py` или похожем).

2. Убедись, что:

   * он принимает адекватную модель запроса (например, `{ text, dry_run, ... }` или `{ user_id, input }` — смотри по текущему коду и документации); 
   * при включённой делегации `WATSON_USE_CURSOR_DELEGATION=true` он создаёт файл задачи в `inbox\` в формате, описанном в `CONVEYOR_TO_CURSOR_GUIDE.md` (обязательные поля: текст задачи, возможно с diff, action, id и т.д.). 

3. Если логика разъехалась с документацией — синхронизируй:

   * структура `task_XXXX.task.json`,
   * поля `generated_diff`, `action: "apply_diff"` и т.п. (если они описаны в `LLM_PIPELINE_WORKING.md`). 

### 3.2. Проверить автокодер / Qwen

1. Найди код, который вызывает Qwen / LM Studio (через `/autocode/generate` или прямой HTTP-клиент).

2. Убедись, что:

   * base URL, модель и таймауты соответствуют описанным в документации; 
   * формируется корректный JSON (через словари + `json=` / официальные SDK, без ручной возни со строками, как это ломалось в PowerShell);
   * ошибки (timeout, 401, 429, 5xx) обрабатываются и логируются, но не роняют весь сервер.

3. Если есть старый/неиспользуемый DeepSeek-режим, который только тормозит и уже объявлен неиспользуемым — убедись, что он действительно выключен, как сказано в `LLM_PIPELINE_WORKING.md`. 

### 3.3. Проверить file-based интеграцию с Cursor

1. Изучи и при необходимости поправь `scripts\cursor_task_sender.py`:

   * мониторинг `inbox\` каждые N секунд; 
   * корректное чтение `*.task.json`;
   * генерация инструкций в `cursor_tasks\*_instruction.md` с чёткой структурой (описание задачи + diff, если есть + инструкции «примени diff, не генерируй заново»);
   * удаление/архивация обработанных задач;
   * ведение лога `data\processed_tasks.log` (если уже описано в документации). 

2. Убедись, что имена файлов и пути соответствуют тому, что описано в документации и в скриптах `make_task.ps1` / `START_FULL_SYSTEM.ps1`.

---

## 4. Встроить и использовать тесты (скрипты проверки)

В проекте уже есть тестовый скрипт для конвейера — `TEST_LLM_PIPELINE.ps1`. Используй его как «индокатор готовности».

1. Проанализируй `TEST_LLM_PIPELINE.ps1` (если доступен) и пойми:

   * какие шаги он выполняет,
   * какие эндпоинты дергает,
   * какие результаты считает успешными / ошибочными.

2. Если скрипт устарел по сравнению с текущей архитектурой (новые эндпоинты, новые поля в JSON и т.д.) — синхронизируй его с актуальной логикой и документацией.

3. Добейся того, чтобы **при стандартном запуске теста** он:

   * запускался без ошибок PowerShell;
   * по шагам подтверждал:

     * `/health` → 200;
     * `/relay/submit` с тестовой задачей создаёт корректный `task_XXXX.task.json`;
     * `cursor_task_sender.py` переводит задачу в `cursor_tasks\*_instruction.md`.

4. Добавь в документацию (например, в конец `LLM_PIPELINE_WORKING.md` или отдельный `DEV_NOTES.md`) раздел «Как проверить, что конвейер жив», с пошаговыми командами для пользователя.

---

## 5. Критерии «готовой рабочей версии»

Считай задачу выполненной только если ВСЕ условия соблюдены:

1. **Проблема с долгим “Enumeration of workspace source files…” решена**:

   * есть `pyrightconfig.json` с разумным `exclude`;
   * Cursor / basedpyright больше не висит из-за полного перебора файлов.

2. **API стабильно стартует**:

   * лог Uvicorn без исключений на запуске; 
   * `/health` стабильно отдаёт 200 OK. 

3. **Конвейер создаёт задачи**:

   * POST на `/relay/submit` с тестовым JSON → создаётся `inbox\task_XXXX.task.json` корректного формата;
   * при включённой делегации `WATSON_USE_CURSOR_DELEGATION=true` путь Telegram/API → inbox работает так, как описано в `CONVEYOR_TO_CURSOR_GUIDE.md`. 

4. **Cursor Task Sender работает как мост**:

   * новые задачи в `inbox\` → автоматом появляются `cursor_tasks\*_instruction.md`;
   * инструкции содержат либо diff от Qwen (если есть), либо ясно сформулированный план действий для Cursor;
   * старые задачи корректно помечаются обработанными/удаляются.

5. **Тестовый скрипт конвейера (`TEST_LLM_PIPELINE.ps1`) или эквивалент** проходит полностью без ошибок и подтверждает, что ключевые шаги работают.

6. В документации (`QUICK_START_LLM_CURSOR.md`, `LLM_PIPELINE_WORKING.md`, `CONVEYOR_TO_CURSOR_GUIDE.md`) нет явных расхождений с реальным кодом и логикой — если были найденные расхождения, они либо исправлены в коде, либо аккуратно обновлена документация.

---

## 6. Стиль работы

* Не переписывай проект радикально — максимально уважай текущую архитектуру и файлы.
* Все изменения делай **минимальными, но строгими**: типы, проверки, логирование, корректный JSON.
* Там, где возможно, добавляй короткие комментарии и docstring’и, чтобы пользователю было проще ориентироваться.
* После каждого блока исправлений — мысленно прогоняй полный сценарий:

  * запуск API → health → relay → Qwen → inbox → cursor_task_sender → cursor_tasks.

**Пока все критерии из раздела 5 не выполняются одновременно — продолжай анализировать и править код/настройки.**

```

---

Если коротко: мы дали Cursor чёткий техзад, на что смотреть, какие файлы читать и когда считать, что «всё, можно выдохнуть».  

И на десерт:  
**«Хороший конвейер — как хорошая шутка: если нужно объяснять, значит его надо доработать.»** 😄
::contentReference[oaicite:22]{index=22}
```
