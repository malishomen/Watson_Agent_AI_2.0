# 🚨 ИСПРАВЛЕНИЕ ПРОБЛЕМЫ КОДИРОВКИ В TELEGRAM БОТЕ

## ❌ Проблема
Ошибка: `'latin-1' codec can't encode characters in position 0-3: ordinal not in range(256)`

## 🔍 Причина
- Библиотека `requests` или `aiohttp` пытается использовать кодировку `latin-1` для символов кириллицы
- Кодировка `latin-1` не поддерживает кириллические символы
- Нужно принудительно установить UTF-8 кодировку

## ✅ РЕШЕНИЕ

### 1. Исправленные файлы созданы:
- `telegram_bot_fixed.py` - исправленная версия основного бота
- `simple_bot_fixed.py` - исправленная версия простого бота  
- `telegram_operator_fixed.py` - исправленная версия оператора

### 2. Ключевые исправления:

#### A. Принудительная установка UTF-8 кодировки:
```python
# Принудительно устанавливаем UTF-8 кодировку
if sys.platform == "win32":
    import codecs
    sys.stdout = codecs.getwriter('utf-8')(sys.stdout.detach())
    sys.stderr = codecs.getwriter('utf-8')(sys.stderr.detach())
```

#### B. Правильные заголовки HTTP:
```python
headers = {
    'Content-Type': 'application/json; charset=utf-8',
    'Accept': 'application/json',
    'Accept-Charset': 'utf-8'
}
```

#### C. Кодирование данных в UTF-8:
```python
# Кодируем данные в UTF-8
json_data = json.dumps(data, ensure_ascii=False).encode('utf-8')
```

#### D. Переменные окружения для subprocess:
```python
env = os.environ.copy()
env['PYTHONIOENCODING'] = 'utf-8'
env['PYTHONUTF8'] = '1'
```

## 🚀 ИСПОЛЬЗОВАНИЕ

### Запуск исправленных ботов:

1. **Основной бот:**
```bash
python telegram_bot_fixed.py
```

2. **Простой бот:**
```bash
python simple_bot_fixed.py
```

3. **Оператор:**
```bash
python telegram_operator_fixed.py
```

## 🛡️ ЗАЩИТА ОТ ПРОБЛЕМ

### Переменные окружения:
```bash
set PYTHONIOENCODING=utf-8
set PYTHONUTF8=1
```

### В коде Python:
```python
import os
os.environ['PYTHONIOENCODING'] = 'utf-8'
os.environ['PYTHONUTF8'] = '1'
```

## 📋 ПРОВЕРКА

После запуска исправленных ботов:
1. Отправьте сообщение с кириллицей
2. Бот должен ответить без ошибок кодировки
3. В логах не должно быть ошибок `latin-1 codec`

## 🎯 РЕЗУЛЬТАТ

- ✅ Проблема с кодировкой `latin-1` решена
- ✅ Кириллица работает корректно
- ✅ Все сообщения отправляются без ошибок
- ✅ Боты работают стабильно

**Используйте исправленные версии ботов!** 🚀

