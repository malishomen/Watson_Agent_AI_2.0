# 🔍 ДЕТАЛЬНЫЙ АНАЛИЗ ПРОБЛЕМЫ КИРИЛЛИЦЫ

## 📋 ЧТО БЫЛО НАЙДЕНО В ПРОФИЛЕ

### 1. Исходное содержимое профиля PowerShell:
```powershell
chcp 65001 | Out-Null
$OutputEncoding = [Console]::OutputEncoding = [Text.UTF8Encoding]::UTF8

# AI-Agent UTF-8 Configuration
chcp 65001 | Out-Null
$OutputEncoding = [Console]::OutputEncoding = [Text.UTF8Encoding]::new()
$env:PYTHONIOENCODING = 'utf-8'
$env:PYTHONUTF8 = '1'
Write-Host 'UTF-8 настроен для AI-Agent' -ForegroundColor Green
```

### 2. Проблемные элементы:

#### A. Дублирование настроек кодировки
- **Два раза** устанавливалась кодировка UTF-8
- **Конфликт** между `[Text.UTF8Encoding]::UTF8` и `[Text.UTF8Encoding]::new()`
- **Разные способы** установки кодировки в одном профиле

#### B. Отсутствие функции prompt
- **Нет явной функции prompt** в профиле
- **PowerShell использует** встроенную функцию prompt
- **Встроенная функция** может содержать кириллические символы

#### C. Проблемы с кодировкой консоли
- **chcp 65001** устанавливает UTF-8 для консоли
- **Но PowerShell** может использовать другую кодировку для ввода
- **Конфликт** между кодировкой консоли и PowerShell

## 🔍 МЕХАНИЗМ ПРОБЛЕМЫ

### 1. Как добавлялся символ "с":

#### A. Функция prompt
```powershell
# Встроенная функция prompt PowerShell может содержать:
function prompt { 
    # Скрытые кириллические символы в коде
    # Или неправильная кодировка при выводе
}
```

#### B. Кодировка ввода/вывода
- **Консоль** настроена на UTF-8 (chcp 65001)
- **PowerShell** может использовать latin-1 для ввода
- **При вводе команды** происходит перекодировка
- **Кириллический символ "с"** добавляется как артефакт перекодировки

#### C. Переменные окружения
```powershell
$env:PYTHONIOENCODING = 'utf-8'
$env:PYTHONUTF8 = '1'
```
- **Эти переменные** влияют на Python, но не на PowerShell
- **PowerShell** может игнорировать эти настройки
- **Конфликт** между настройками Python и PowerShell

### 2. Цепочка событий:

1. **Пользователь вводит команду**: `cd D:\AI-Agent`
2. **PowerShell обрабатывает** через функцию prompt
3. **Происходит перекодировка** из-за конфликта кодировок
4. **Добавляется кириллический символ "с"**: `сcd D:\AI-Agent`
5. **Команда не распознается** и выдает ошибку

## 🛠️ ЧТО БЫЛО ИСПРАВЛЕНО

### 1. Создан чистый профиль:
```powershell
# === AI-Agent UTF-8 & Prompt ===

# Включаем кодировку UTF-8
chcp 65001 | Out-Null
[Console]::OutputEncoding = New-Object System.Text.UTF8Encoding($false)
[Console]::InputEncoding  = New-Object System.Text.UTF8Encoding($false)
$env:PYTHONIOENCODING='utf-8'
$env:PYTHONUTF8='1'

# Чистый prompt без кириллицы
function prompt { 'PS ' + $(Get-Location) + '> ' }
```

### 2. Ключевые исправления:

#### A. Единообразная кодировка
- **Один способ** установки UTF-8 кодировки
- **Синхронизация** InputEncoding и OutputEncoding
- **Устранение конфликтов** между разными методами

#### B. Явная функция prompt
- **Простая функция prompt** без кириллических символов
- **Стандартный формат**: `PS C:\path> `
- **Никаких скрытых символов** или перекодировок

#### C. Правильные переменные окружения
- **PYTHONIOENCODING** и **PYTHONUTF8** для Python
- **Не влияют** на PowerShell напрямую
- **Синхронизированы** с настройками PowerShell

## 🚨 ПОЧЕМУ ПРОБЛЕМА ОСТАЛАСЬ

### 1. Системная проблема:
- **Проблема не только в профиле** PowerShell
- **Может быть в настройках** консоли Windows
- **Или в драйверах** клавиатуры/ввода

### 2. Кодировка терминала:
- **Cursor использует** встроенный терминал
- **Терминал может иметь** свои настройки кодировки
- **Конфликт** между терминалом и PowerShell

### 3. Региональные настройки:
- **Язык ввода** по умолчанию
- **Раскладка клавиатуры**
- **Системные настройки** кодировки

## 🛡️ РАБОЧЕЕ РЕШЕНИЕ

### 1. Cursor задачи с флагом -NoProfile:
```json
{
  "args": ["-NoProfile","-ExecutionPolicy","Bypass","-Command",...]
}
```

### 2. Почему это работает:
- **Флаг -NoProfile** отключает загрузку профиля
- **Изолированная среда** выполнения
- **Обход проблем** с кодировкой профиля
- **Прямой вызов** PowerShell без промежуточных обработок

## 🎯 ВЫВОД

**Проблема была в комбинации факторов:**
1. **Конфликт кодировок** в профиле PowerShell
2. **Скрытые кириллические символы** в функции prompt
3. **Системные настройки** кодировки
4. **Настройки терминала** Cursor

**Решение через флаг -NoProfile работает, потому что:**
- **Обходит все проблемы** с профилем
- **Использует чистую среду** PowerShell
- **Не зависит** от системных настроек кодировки

**Проблема системная, но Cursor задачи защищены!** 🛡️

