# 🤖 Telegram Bot Setup - Настройка Telegram бота

## 📋 Инструкция по настройке

**Telegram бот для AI Agent + Cursor Automation**

### 🔧 Шаг 1: Создание бота

1. **Откройте Telegram** и найдите @BotFather
2. **Отправьте команду** `/newbot`
3. **Введите имя бота** (например: "AI Agent Bot")
4. **Введите username бота** (например: "ai_agent_bot")
5. **Скопируйте токен** (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 🔑 Шаг 2: Настройка токена

```powershell
# Установите токен бота
$env:TELEGRAM_BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"

# Или создайте файл .env
echo "TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE" > .env
```

### 👥 Шаг 3: Настройка пользователей

```powershell
# Установите ID пользователей (через запятую)
$env:ALLOWED_USERS = "123456789,987654321"

# Или добавьте в .env
echo "ALLOWED_USERS=123456789,987654321" >> .env
```

### 🚀 Шаг 4: Запуск бота

```powershell
# Запуск через PowerShell скрипт
.\start_bot.ps1

# Или прямой запуск Python
python telegram_bot.py
```

### 📱 Шаг 5: Тестирование

1. **Найдите вашего бота** в Telegram
2. **Отправьте команду** `/start`
3. **Проверьте статус** `/status`
4. **Создайте задачу** `/task Создать веб-приложение`

### 🛠️ Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/help` | Показать справку |
| `/status` | Проверить статус системы |
| `/task <описание>` | Создать новую задачу |

### 📝 Примеры использования

```
/start
/status
/task Создать REST API на FastAPI
/task Добавить аутентификацию
/task Создать веб-интерфейс
```

### 🔍 Диагностика

#### Проверка API агента:
```powershell
curl http://127.0.0.1:8088/health
```

#### Проверка логов:
```powershell
Get-Content telegram_bot.log -Tail 20
```

#### Проверка процессов:
```powershell
Get-Process | Where-Object {$_.ProcessName -eq "python"}
```

### ⚠️ Устранение проблем

#### Бот не отвечает:
1. Проверьте токен: `$env:TELEGRAM_BOT_TOKEN`
2. Проверьте API агент: `curl http://127.0.0.1:8088/health`
3. Проверьте логи: `Get-Content telegram_bot.log`

#### Ошибка "Доступ запрещен":
1. Добавьте ваш Telegram ID в `ALLOWED_USERS`
2. Получите ID через @userinfobot

#### API агент не работает:
```powershell
# Запустите API агент
python -m uvicorn api.agent:app --host 127.0.0.1 --port 8088
```

### 🎯 Результат

**Telegram бот готов к работе!**

- ✅ **Бот создан** и настроен
- ✅ **Токен установлен** и работает
- ✅ **API агент** интегрирован
- ✅ **Автоматизация Cursor** готова
- ✅ **Пользователи** настроены

**Бот готов принимать задачи и автоматически выполнять их в Cursor!** 🚀

---

*Telegram бот для AI Agent + Cursor Automation - полная настройка*





