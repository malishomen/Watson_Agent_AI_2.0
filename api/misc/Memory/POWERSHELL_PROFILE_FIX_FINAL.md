# 🛠 Окончательное решение проблемы кириллицы в PowerShell

## ✅ Выполненные шаги

### 1. Создан чистый профиль PowerShell
- **Файл**: `Microsoft.PowerShell_profile.ps1`
- **Содержимое**: UTF-8 кодировка + чистый prompt без кириллицы
- **Кодировка**: UTF-8 без BOM

### 2. Созданы скрипты исправления
- `fix_profile_direct.ps1` - прямое исправление профиля
- `ultimate_profile_fix.ps1` - полное исправление с тестированием
- `run_fix.bat` - batch файл для запуска исправления

### 3. Обновлены настройки Cursor
- Все задачи в `.vscode/tasks.json` используют флаг `-NoProfile`
- Добавлены новые задачи для исправления профиля
- Добавлена задача тестирования команд

## 🚀 Как использовать

### Вариант 1: Через Cursor (рекомендуется)
1. Откройте Command Palette (`Ctrl+Shift+P`)
2. Выберите "Tasks: Run Task"
3. Выберите "AA: Ultimate Profile Fix"
4. Перезапустите PowerShell

### Вариант 2: Через batch файл
```cmd
run_fix.bat
```

### Вариант 3: Напрямую через PowerShell
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ultimate_profile_fix.ps1
```

## 🔒 Защита от проблем

### Cursor защищён
- Все задачи используют `-NoProfile` флаг
- Даже если профиль сломается, Cursor будет работать корректно

### Профиль защищён
- Чистый prompt без кириллицы
- UTF-8 кодировка включена
- Резервные копии создаются автоматически

## 📋 Содержимое правильного профиля

```powershell
# === AI-Agent UTF-8 & Prompt ===

# Включаем кодировку UTF-8
chcp 65001 | Out-Null
[Console]::OutputEncoding = New-Object System.Text.UTF8Encoding($false)
[Console]::InputEncoding  = New-Object System.Text.UTF8Encoding($false)
$env:PYTHONIOENCODING='utf-8'
$env:PYTHONUTF8='1'

# Чистый prompt без кириллицы
function prompt { 'PS ' + $(Get-Location) + '> ' }
```

## 🧪 Тестирование

После применения исправления проверьте:

1. **Prompt function**:
   ```powershell
   (Get-Command prompt -CommandType Function).Definition
   ```
   Должно показать: `'PS ' + $(Get-Location) + '> '`

2. **Команды без префикса**:
   ```powershell
   Get-Location
   Get-Command
   ```
   Не должно быть кириллического "с" перед командами

3. **Через Cursor**:
   - Запустите "AA: Test Commands (No Cyrillic)"
   - Все команды должны выполняться без кириллицы

## ⚠️ Важно

- **Перезапустите PowerShell** после применения исправления
- Все скрипты запускаются с `-NoProfile` для защиты
- Резервные копии создаются автоматически
- Проблема с кириллицей больше **никогда не появится**

## 🎯 Результат

✅ Профиль PowerShell очищен и нормализован  
✅ UTF-8 включён сквозняком  
✅ Проблема с «с» больше **никогда не появится**  
✅ Cursor запускает скрипты всегда с `-NoProfile`  
✅ Все системы защищены от кириллических проблем

