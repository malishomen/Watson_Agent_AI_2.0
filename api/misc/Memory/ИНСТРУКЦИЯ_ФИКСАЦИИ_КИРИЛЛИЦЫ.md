# 🚀 ИНСТРУКЦИЯ ФИКСАЦИИ КИРИЛЛИЦЫ - РАЗ И НАВСЕГДА

## ✅ ВЫПОЛНЕНО:

### 1. Создан фикс-скрипт профилей PowerShell
- **Файл**: `D:\AI-Agent\scripts\fix_ps_profile.ps1`
- **Функция**: Полная очистка проблемных профилей + минимальный безопасный UTF-8 prompt

### 2. Обновлены настройки VS Code для безопасного терминала
- **Файл**: `.vscode/settings.json`
- **Настройки**: Отключен shell-integration, безопасный профиль PowerShell

### 3. Добавлены задачи Cursor для чистой PowerShell
- **Файл**: `.vscode/tasks.json`
- **Задачи**: AA: Fix PS Profile, AA: Clean PS Cache, AA: Start API (NoProfile)

## 🎯 СЛЕДУЮЩИЕ ШАГИ:

### ШАГ 1: Запуск фиксации
1. **Откройте Command Palette**: `Ctrl+Shift+P`
2. **Выберите**: "Tasks: Run Task"
3. **Выберите**: "AA: Fix PS Profile"
4. **Ожидайте**: Зелёное сообщение "✅ Профиль PowerShell очищен..."

### ШАГ 2: Откройте новую вкладку терминала
1. **Откройте новую вкладку** терминала PowerShell (важно!)
2. **Наберите вручную**: `cd D:\AI-Agent`
3. **Ожидайте**: Никакой "с" перед командой, переход директории успешен

### ШАГ 3: Верификация решения
В новой вкладке PowerShell выполните:

```powershell
cd D:\AI-Agent
.\venv\Scripts\Activate.ps1
$PSVersionTable.PSVersion
$PROFILE
Get-Content $PROFILE
```

**Ожидаемые результаты:**
- ✅ `cd` выполняется без добавления "с"
- ✅ Профиль содержит "CLEAN UTF-8 PROFILE"
- ✅ В начале профиля нет случайных символов/"с"
- ✅ Нет `chcp`, `Write-Host`, "украшателей"

### ШАГ 4: Дополнительная проверка
```powershell
dir
powershell -NoProfile -Command "Write-Host OK"
cmd /c echo works
```

**Всё должно работать без префикса "с".**

## 🛡️ ЗАЩИТА ОТ ВОЗВРАТА ПРОБЛЕМЫ:

### ✅ Всегда используйте:
- **Cursor задачи с флагом -NoProfile** для важных операций
- **Минимальный профиль** без украшателей
- **ASCII-безопасный prompt** (только латиница/цифры)

### ❌ Избегайте:
- Добавления `chcp 65001` в профили
- Подключения oh-my-posh/starship в VS Code профиле
- Изменения настроек shell-integration

## 🎉 РЕЗУЛЬТАТ:

**После выполнения всех шагов:**
- ✅ Кириллический символ "с" больше не добавляется к командам
- ✅ Все команды работают корректно
- ✅ Профиль PowerShell очищен и безопасен
- ✅ Cursor задачи защищены флагом -NoProfile

**Проблема решена раз и навсегда!** 🚀

