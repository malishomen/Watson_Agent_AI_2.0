# 🚀 AI-Agent × Cursor — Финальный Пакет

## 📁 Структура пакета

```
Memory/
├── docs/
│   └── cursor.md                    # Полная документация
├── .vscode/
│   ├── settings.json               # Переменные окружения
│   └── tasks.json                   # Готовые задачи для Cursor
├── scripts/
│   ├── start_agent.ps1             # Запуск API агента
│   └── run_cursor_command.ps1       # Выполнение команд
└── CURSOR_PACKAGE_README.md         # Этот файл
```

## ⚡ Быстрые тесты в Cursor

### 1. Настройка секрета
Открой `.vscode/settings.json` и замени `REPLACE_WITH_YOUR_SECRET` на свой секрет.

### 2. Запуск API
```
Ctrl+Shift+P → Tasks: Run Task → AA: Start API
```

### 3. Проверка здоровья
```
Ctrl+Shift+P → Tasks: Run Task → AA: Health Check
```
**Ожидаемый результат:** `{ "status":"ok" }`

### 4. Тест команды
```
Ctrl+Shift+P → Tasks: Run Task → AA: Command (prompt)
```
**Введите:** `где я`
**Ожидаемый результат:** Текущий `WORKDIR`

### 5. Проверка заявок
```
Ctrl+Shift+P → Tasks: Run Task → AA: Approvals Pending
```

### 6. Валидация проекта
```
Ctrl+Shift+P → Tasks: Run Task → AA: Project Validate (prompt path)
```
**Введите:** `D:/AI-Agent/Projects/demo/ProjectSpec.yml`

### 7. Запуск проекта
```
Ctrl+Shift+P → Tasks: Run Task → AA: Project Run (prompt id)
```
**Введите:** `demo`

### 8. Статус проекта
```
Ctrl+Shift+P → Tasks: Run Task → AA: Project Status (prompt id)
```
**Введите:** `demo`

## 🎯 Примеры команд

### Базовые команды
- `где я` → показать текущую директорию
- `запусти notepad` → открыть блокнот
- `список файлов` → показать файлы в директории

### Файловые операции
- `прочитай D:\AI-Agent\README.md` → прочитать файл
- `запиши в D:\AI-Agent\test.txt: Привет мир` → записать в файл
- `удали D:\AI-Agent\temp.txt` → удалить файл

### Проектные операции
- `запусти проект demo` → запустить проект
- `статус проекта demo` → статус проекта
- `останови проект demo` → остановить проект

## 🔧 Настройка

### Переменные окружения
В `.vscode/settings.json`:
```json
{
  "terminal.integrated.env.windows": {
    "AGENT_API_BASE": "http://127.0.0.1:8088",
    "AGENT_HTTP_SHARED_SECRET": "ВАШ_СЕКРЕТ"
  }
}
```

### Запуск через скрипт
```powershell
# Прямой запуск API
.\scripts\start_agent.ps1 -Secret "ВАШ_СЕКРЕТ"

# Выполнение команды
.\scripts\run_cursor_command.ps1 -Text "где я"
```

## 🛠️ Устранение неполадок

### API не запускается
1. Проверьте, что порт 8088 свободен
2. Убедитесь, что `python-multipart` установлен
3. Проверьте переменную `AGENT_HTTP_SHARED_SECRET`

### Команды не выполняются
1. Проверьте, что API запущен
2. Убедитесь в правильности секрета
3. Проверьте белые списки директорий

### Проблемы с кодировкой
Скрипты автоматически настраивают UTF-8 кодировку.

## 🎉 Готово!

Теперь у вас есть полный пакет для интеграции AI-Agent с Cursor:
- ✅ Готовые задачи в Cursor
- ✅ Автоматическая настройка окружения
- ✅ Безопасное выполнение команд
- ✅ Полная документация

**Начните с:** `Ctrl+Shift+P → Tasks: Run Task → AA: Start API`

