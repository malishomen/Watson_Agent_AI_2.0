# Watson AI Agent - Система запуска

## 🚀 Быстрый запуск

### Основные файлы:

1. **`WatsonAgent.bat`** - Основной файл запуска системы
2. **`WatsonAgent_Advanced.bat`** - Расширенный запуск с диагностикой
3. **`WatsonAgent_Stop.bat`** - Остановка всех служб
4. **`WatsonAgent_Test.bat`** - Тестирование системы

## 📋 Компоненты системы

- **FastAPI сервер** - http://127.0.0.1:8088
- **Telegram бот** - @watson_agent_bot
- **LM Studio** - http://127.0.0.1:1234
- **AI Agent** - Обработка команд через LM Studio

## 🔧 Использование

### Запуск системы:
```batch
WatsonAgent.bat
```

### Расширенный запуск (с диагностикой):
```batch
WatsonAgent_Advanced.bat
```

### Тестирование:
```batch
WatsonAgent_Test.bat
```

### Остановка:
```batch
WatsonAgent_Stop.bat
```

## ⚙️ Настройки

### Переменные окружения (в файлах .bat):
- `AGENT_HTTP_SHARED_SECRET=test123`
- `TG_BOT_TOKEN=7073794782:AAGyvdUWJgpt8QvIdl9uV6seyYhl83ndalk`
- `PYTHONUTF8=1`

### Конфигурация (config.env):
```
TG_BOT_TOKEN=7073794782:AAGyvdUWJgpt8QvIdl9uV6seyYhl83ndalk
AGENT_HTTP_SHARED_SECRET=test123
AGENT_API_BASE=http://127.0.0.1:8088
LM_STUDIO_URL=http://127.0.0.1:1234
```

## 🧪 Тестирование

### API тест:
```powershell
$body = @{ text = "Привет!"; session = "Test" } | ConvertTo-Json
$response = Invoke-RestMethod -Uri "http://127.0.0.1:8088/command" -Method Post -Headers @{"x-agent-secret"="test123"; "Content-Type"="application/json"} -Body $body
$response.result
```

### Проверка здоровья:
```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8088/health" -Headers @{"x-agent-secret"="test123"}
```

## 📱 Telegram бот

- **Имя**: @watson_agent_bot
- **Команды**: 
  - `/start` - Начать работу
  - `/ping` - Проверка связи
  - `/status` - Статус системы
  - Любое сообщение - Обработка агентом

## 🔍 Диагностика

### Проверка портов:
```batch
netstat -ano | findstr :8088
netstat -ano | findstr :1234
```

### Проверка процессов:
```batch
tasklist | findstr python.exe
```

### Логи:
- API сервер: Окно "Watson API Server"
- Telegram бот: Окно "Watson Telegram Bot"

## 🛠️ Устранение проблем

### API не запускается:
1. Проверьте, что порт 8088 свободен
2. Убедитесь, что Python установлен
3. Проверьте зависимости: `pip install fastapi uvicorn requests`

### Telegram бот не отвечает:
1. Проверьте токен в config.env
2. Убедитесь, что интернет работает
3. Проверьте, что API сервер запущен

### LM Studio недоступен:
1. Запустите LM Studio
2. Загрузите модель
3. Убедитесь, что сервер работает на порту 1234

## 📊 Статус системы

✅ **Работает**: FastAPI, Telegram бот, LM Studio
✅ **Тестировано**: API интеграция, команды агента
✅ **Настроено**: Токены, переменные окружения
✅ **Готово**: Полная система Watson AI Agent

## 🎯 Результат

Система полностью настроена и готова к работе:
- Агент отвечает на команды через API
- Telegram бот интегрирован
- LM Studio обрабатывает запросы
- Все компоненты работают вместе
