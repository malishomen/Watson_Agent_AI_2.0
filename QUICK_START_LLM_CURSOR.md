# 🚀 БЫСТРЫЙ СТАРТ: Qwen 2.5 → Cursor Pipeline

## ✅ Система готова и работает!

**Логика:** Telegram → Qwen 2.5 Coder → Cursor применяет готовый diff

---

## 📝 ИНСТРУКЦИЯ НА 5 МИНУТ

### Шаг 1: Запуск системы (один раз)

```powershell
cd D:\projects\Ai-Agent_Watson\Watson_Agent_2.0

# Запуск Watson API
pwsh scripts\Start-WatsonApi.ps1 -Port 8090

# В новом окне: Telegram Bridge
py -3.11 -X utf8 scripts\telegram_bridge.py

# В новом окне: Cursor Task Sender  
py -3.11 -X utf8 scripts\cursor_task_sender.py
```

**Результат:**
```
✅ Watson API: Running (8090)
✅ Telegram Bridge: Listening
✅ Cursor Task Sender: Watching inbox/
```

---

### Шаг 2: Отправить задачу боту

Напишите в Telegram боту:
```
Добавь комментарий JSDoc к функции loadUserTree
```

**Что происходит:**
1. ⏳ Qwen 2.5 генерирует diff (~5-10 сек)
2. ✅ Создается inbox/task_XXXX.task.json
3. ✅ Cursor Task Sender создает инструкцию
4. 📂 Файл: `cursor_tasks/task_XXXX_instruction.md`

---

### Шаг 3: Выполнить в Cursor

**Откройте файл:**
```powershell
# Автоматически откроется или вручную:
Invoke-Item cursor_tasks\task_XXXX_instruction.md
```

**В Cursor IDE:**
1. `Ctrl+A` - выделить весь текст
2. `Ctrl+L` - открыть Cursor Chat
3. `Ctrl+V` - вставить
4. `Enter` - отправить

**Cursor AI прочитает и применит готовый diff от Qwen!**

---

## 🎯 ЧТО ПОЛУЧИТЕ

### В файле инструкции увидите:

```markdown
# 🎯 ЗАДАЧА С ГОТОВЫМ DIFF ОТ WATSON LLM

## Qwen 2.5 Coder Generated Diff:
```diff
--- a/App.jsx
+++ b/App.jsx
@@ -125,6 +125,10 @@
+  /**
+   * Загружает дерево пользователя
+   */
   const loadUserTree = useCallback(async () => {
```

## Инструкции для Cursor:
**НЕ генерируй код заново!**
Примени готовый diff выше.
```

---

## 💡 Примеры задач

Отправьте боту любую из:

```
Добавь логирование в функцию savePosition
```

```
Добавь type hints в модуль authentication
```

```
Рефакторинг: вынеси валидацию в отдельную функцию
```

```
Исправь баг: сообщение перекрывает кнопки
```

**Каждая задача:**
1. Обработается Qwen за 5-10 сек
2. Создаст готовый diff
3. Передаст в Cursor для применения

---

## 📊 Преимущества

| Было | Стало |
|------|-------|
| DeepSeek R1 + Qwen | Только Qwen |
| Timeout >60 сек | ~5-10 сек ⚡ |
| Cursor генерирует сам | Cursor применяет готовое ✅ |
| Два источника кода | Один источник (Qwen) 🎯 |

---

## 🔧 Настройки

### Включить делегацию:

```powershell
[Environment]::SetEnvironmentVariable('WATSON_USE_CURSOR_DELEGATION','true','User')
```

### Выключить делегацию:

```powershell
[Environment]::SetEnvironmentVariable('WATSON_USE_CURSOR_DELEGATION','false','User')

# После этого: задачи будут выполняться API напрямую (без Cursor)
```

---

## 🛟 Troubleshooting

### Задачи не попадают в inbox

```powershell
# Проверьте переменную:
$env:WATSON_USE_CURSOR_DELEGATION  # должно быть 'true'

# Перезапустите API:
pwsh scripts\Start-WatsonApi.ps1 -Port 8090
```

### Инструкции не создаются

```powershell
# Запущен ли Cursor Task Sender?
Get-Process python | Where-Object { 
    (Get-CimInstance Win32_Process -Filter "ProcessId = $($_.Id)").CommandLine -like "*cursor_task_sender*"
}

# Если нет - запустите:
py -3.11 -X utf8 scripts\cursor_task_sender.py
```

### Qwen генерирует плохой diff

```powershell
# Проверьте что модель загружена в LM Studio:
curl http://127.0.0.1:1234/v1/models

# Должна быть: qwen2.5-coder-7b-instruct
```

---

## 📂 Готовые инструкции

В папке `cursor_tasks/` уже есть примеры:

```
task_DEMO.task_instruction.md  ← С готовым diff!
```

**Откройте и протестируйте прямо сейчас!**

---

## 🎉 ИТОГО

**✅ Система Qwen 2.5 → Cursor полностью работает!**

**Что делать:**
1. Отправьте задачу боту в Telegram
2. Дождитесь создания инструкции (~10-15 сек)
3. Откройте файл в Cursor
4. Отправьте в Chat
5. Наслаждайтесь результатом! 🚀

**Документация:** `LLM_PIPELINE_WORKING.md`  
**GitHub:** https://github.com/malishomen/Watson_Agent_AI_2.0


